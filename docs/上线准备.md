# Lilovideo 上线准备

> 面向部署者的操作清单与架构说明。基于代码现状整理，2026-09-24。

## 一、架构与端口

**生产形态是单端口**：前端静态导出（`NEXT_OUTPUT=export npm run build` → `src/frontend/out/`），
由 FastAPI 挂在同一个端口托管（见 `api/app.py` 末尾的 mount）。

| 层 | 技术 | 端口 | 说明 |
|---|---|---|---|
| 应用（前端 + 接口） | FastAPI 托管静态产物 + API | **8000** | 同源，一个进程、一个地址 |
| 前端（仅开发） | Next.js 16（React 19 / Tailwind v4 / TS） | 3000 | `next dev`，带热更新 |
| 模型 | 方舟 / DashScope 等（config.yaml） | — | 后端直连 |

启动：

```bash
bash scripts/serve.sh     # 单端口（使用）
bash scripts/launch.sh    # 双端口（开发，前端热更新）
bash scripts/stop.sh      # 停止
```

**为什么改成单端口**：前端原先靠 `next.config.ts` 的 `rewrites` 把 30 条 `/api/*`
逐条代理到后端，而这套代理**漏配过三次**（`/api/cost/*`、`/api/credits/*`、`/api/models/*`），
症状都是「直连后端 200、经前端 404」，排查时极易误判成后端问题。
同源之后这类问题从根上不存在，同时不需要 CORS、运行时也不需要 Node。

> 开发模式（:`3000`）仍走 rewrites，所以 `test_proxy_coverage.py` 继续守这条线——
> 但它的危害范围已经降级为"只影响开发"。

## 二、启动方式

### 单端口（使用）
```bash
bash scripts/serve.sh       # 首次会自动构建前端；前端与接口都在 :8000
```

### 本地开发（双端口 + 热更新）
```bash
bash scripts/launch.sh      # 前端 :3000 + 后端 :8000
```
日志：`/tmp/lilovideo-api.log`、`/tmp/lilovideo-web.log`
停止：`kill $(cat /tmp/lilovideo-api.pid) $(cat /tmp/lilovideo-web.pid)`

### 生产构建 + 运行
```bash
cd src/frontend && npm run build   # 生成 .next
bash scripts/serve.sh              # 单端口（推荐）
```
首次搭建：
```bash
cd src/backend && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
cd src/frontend && npm install
```

## 三、密钥与配置（上线安全，代码已内置两道防线，见 `src/backend/deployment.py`）

1. **环境变量优先**：线上密钥只放服务端环境变量，不写进可写的 `config.yaml`。映射：

| 环境变量 | 配置点 |
|---|---|
| `DASHSCOPE_API_KEY` | api_providers.dashscope.api_key |
| `ARK_API_KEY` | api_providers.ark.api_key |
| `OPENAI_API_KEY` | api_providers.openai.api_key |
| `GEMINI_API_KEY` | api_providers.gemini.api_key |
| `DEEPSEEK_API_KEY` | api_providers.deepseek.api_key |
| `KLING_ACCESS_KEY` / `KLING_SECRET_KEY` | api_providers.kling.* |

2. **公网自动禁写配置**：监听 `0.0.0.0` 时（public 模式）配置写接口自动 403，`base_url` 不可被外部篡改。本地回环（127.0.0.1）照常可改。

## 四、上线前检查清单

- [x] `npm run build` 通过（生产构建，Next 16.3 全页静态生成 OK）
- [x] `scripts/serve.sh` 单端口可起，`:8000` 同时提供界面与接口（2026-09-25 实测）
- [x] `scripts/launch.sh` 双端口可起，`:3000`/`:8000` health 正常
- [ ] 密钥通过环境变量注入（不用 config.yaml 明文）
- [x] 页面检查：首页参数条模型可加载、三个 pipeline 表单可提交、任务状态轮询正常
- [x] 真实任务验证：2026-09-24 跑通 `standard/image_concat` 全链路（21.6s：storyboard → 图片 → TTS → ffmpeg 拼接 → final.mp4，经 :3000 代理可播放/下载）
- [ ] 域名/HTTPS 反代：Nginx/Caddy 将 **8000** 反代到公网域名（单端口，前后端同源，无需 CORS 配置）
- [ ] 后端公网部署：监听 0.0.0.0 以启用 public 模式安全防线（`config.yaml server.host`）

## 五、已知注意点

- 前端 dev 用 `--webpack` 更稳（README 提示 lightningcss 原生模块问题）；build 不受影响
- **ffmpeg 依赖**：后端视频拼接需要 ffmpeg。ffmpeg 由 `src/install.sh` 下载到 `src/backend/bin/`（evermeet 构建），`api_server.py` 启动时自动加入 PATH。**该二进制不随仓库分发**（单个约 77MB，进 git 历史无法回收）。注意 evermeet 提供的是 **x86_64** 构建（Apple Silicon 上经 Rosetta 运行）；换架构需换对应静态构建（https://evermeet.cx/ffmpeg/ 或 BtbN 构建），或系统安装后无需内置
- 后端配置含真实密钥时，启动真实任务会消耗对应模型配额/积分（本次链路验证消耗少量 TTS/图片配额）
- 任务产物目录：后端 `code/result/task/<task_id>/`，前端经 `/code/*` 代理直接访问；上线前确认静态产物通过反代/对象存储可访问
